# Copy to `.env` and fill in. Then `source .env` before running monitor.py.

# Anthropic (get from console.anthropic.com)
export ANTHROPIC_API_KEY="sk-ant-..."

# Gmail App Password (NOT your Gmail login password).
# Create at: https://myaccount.google.com/apppasswords  (requires 2FA on the account)
export SMTP_PASSWORD="abcd efgh ijkl mnop"
